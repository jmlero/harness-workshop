# Instruction modules

Keep extended agent instructions as separate, descriptively named Markdown files
in this directory. Use one file per concern and link each file from the `Agent
instructions` section of the root `AGENTS.md` instead of embedding its contents.
