interface:
  display_name: "Commit work"
  short_description: "Create a focused, validated Git commit"
  default_prompt: "Use $commit-work to review, validate, and create a focused commit for my current changes."
policy:
  allow_implicit_invocation: false
